#include <stdbool.h>
#include <string.h>
// // should include this for using boolean in code


// // variable = (condition) ? expressionTrue : expressionFalse;
// // this is used instead of else and if conditions,

// // like for example :
// // int time = 20;
// // if (time < 18) {
// //   printf("Good day.");
// // } else {
// //   printf("Good evening.");
// // }

// // we can write:
// // int time = 20;
// // (time < 18) ? printf("Good day.") : printf("Good evening.");

// // important:: for continue we should add the addition or substraction of the integer back before continue in a while loop

// // for using string functions add #include <string.h> in the header
// // use strlen() to know the actual length of the string
// // sizeof() gives the storage bytes and not the actual length