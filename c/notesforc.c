#include <stdbool.h>>
// should include this for using boolean in code


// variable = (condition) ? expressionTrue : expressionFalse;
// this is used instead of else and if conditions,

// like for example :
// int time = 20;
// if (time < 18) {
//   printf("Good day.");
// } else {
//   printf("Good evening.");
// }

// we can write:
// int time = 20;
// (time < 18) ? printf("Good day.") : printf("Good evening.");